# Tool Guardian is mostly local / computation-only.
# No external API keys required for core features.

# Optional: where to write persistent audit logs (future extension)
# AUDIT_LOG_PATH=./audit.jsonl

# Optional: max string length before truncation
# MAX_STRING_LENGTH=4000
